3. implement a non-optimized web page (single-file) with as much possible bugs (slow queries, fake ajax request, wrong image links, etc)
hw expected result: txt report with bugs & descriptions

Created page with bugs:

- non optimized image
- image with fake src
- js code on top
- jquery lounched but never used
- the same css

